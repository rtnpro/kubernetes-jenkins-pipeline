pipeline {
    agent {
      kubernetes {
        label 'jenkins-pipeline'
        containerTemplate {}
      }
    }
    stages {
        stage('Example Build') {
            steps {
                echo 'Hello, world'
            }
        }
    }
}
